import styled from 'styled-components'

import Icon from '../Icon'

export const LayoutWrapper = styled.main`
  width: 100%;
  height: 100%;
  overflow: hidden;
`

export const LayoutRings = styled(Icon)`
  position: fixed;
  bottom: 20px;
  right: 120px;
  opacity: 0.05;
`
